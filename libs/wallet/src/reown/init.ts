// Re-export from wagmi config so config and WagmiProvider share the same types (avoids Config version mismatch).
export { reownAppKit, wagmiAdapter, config as reownWagmiConfig } from '../wagmi/config'
