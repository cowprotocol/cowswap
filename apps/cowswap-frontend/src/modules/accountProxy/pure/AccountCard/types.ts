export enum AccountCardHoverBehavior {
  NONE = 'none',      // No hover effects
  SELF = 'self',      // Hover effects on direct hover only  
  PARENT = 'parent'   // Hover effects when parent is hovered
}