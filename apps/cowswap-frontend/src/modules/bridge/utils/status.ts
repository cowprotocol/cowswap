/**
 * Possible statuses for bridge/swap stops
 */
export enum StopStatusEnum {
  DEFAULT = 'default',
  DONE = 'done',
  PENDING = 'pending',
  FAILED = 'failed',
  REFUND_COMPLETE = 'refund_complete',
}
