import type { SxProps, Theme } from '@mui/material/styles'

export const iconButtonSx: SxProps<Theme> = {
  borderRadius: 1,
  border: '1px solid',
  borderColor: 'divider',
  height: 40,
  width: 40,
}
