{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd ../../ && pnpm run build:widget",
  "outputDirectory": "../../build/widget-configurator",
  "installCommand": "cd ../../ && corepack enable && corepack prepare pnpm@10.30.3 --activate && pnpm run install:ci"
}
