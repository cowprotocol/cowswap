REACT_APP_ENVIRONMENT=local
# Used by Vite when running the dev server (mode=development).
# Ensures NODE_ENV=development and avoids "NODE_ENV=production is not supported" warning.
NODE_ENV=development

# Analytics
#REACT_APP_GOOGLE_ANALYTICS_ID=

REACT_APP_ENVIRONMENT=local
