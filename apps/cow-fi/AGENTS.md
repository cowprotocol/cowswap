# cow-fi AGENTS.md

This file is additive. Follow the repo root `AGENTS.md` for full rules.

## App commands
- Start dev server: `pnpm start:cowfi`
- Build: `pnpm build:cowfi` (includes `postbuild:cowfi` for sitemap)
- Lint: `pnpx nx run cow-fi:lint`
- Test: `pnpx nx run cow-fi:test`
