diff --git a/dist/esm/connectors/injected.js b/dist/esm/connectors/injected.js
index 9501be181ceaecb121326b72dba8fa01ab429f7d..9558a74b61f9db80c641ae91e96762d1d116dcc1 100644
--- a/dist/esm/connectors/injected.js
+++ b/dist/esm/connectors/injected.js
@@ -53,12 +53,6 @@ export function injected(parameters = {}) {
                     connect = this.onConnect.bind(this);
                     provider.on('connect', connect);
                 }
-                // We shouldn't need to listen for `'accountsChanged'` here since the `'connect'` event should suffice (and wallet shouldn't be connected yet).
-                // Some wallets, like MetaMask, do not implement the `'connect'` event and overload `'accountsChanged'` instead.
-                if (!accountsChanged) {
-                    accountsChanged = this.onAccountsChanged.bind(this);
-                    provider.on('accountsChanged', accountsChanged);
-                }
             }
         },
         async connect({ chainId, isReconnecting, withCapabilities } = {}) {
@@ -169,20 +163,9 @@ export function injected(parameters = {}) {
                 connect = this.onConnect.bind(this);
                 provider.on('connect', connect);
             }
-            // Experimental support for MetaMask disconnect
-            // https://github.com/MetaMask/metamask-improvement-proposals/blob/main/MIPs/mip-2.md
-            try {
-                // Adding timeout as not all wallets support this method and can hang
-                // https://github.com/wevm/wagmi/issues/4064
-                await withTimeout(() =>
-                // TODO: Remove explicit type for viem@3
-                provider.request({
-                    // `'wallet_revokePermissions'` added in `viem@2.10.3`
-                    method: 'wallet_revokePermissions',
-                    params: [{ eth_accounts: {} }],
-                }), { timeout: 100 });
-            }
-            catch { }
+            // wallet_revokePermissions revokes MetaMask permissions for the entire origin
+            // (not per-tab), disconnecting all other open tabs. Skip it;
+            // shimDisconnect flag is sufficient to prevent reconnect on next page load.
             // Add shim signalling connector is disconnected
             if (shimDisconnect) {
                 await config.storage?.setItem(`${this.id}.disconnected`, true);
@@ -375,7 +358,12 @@ export function injected(parameters = {}) {
                 throw new SwitchChainError(error);
             }
         },
         async onAccountsChanged(accounts) {
+            // Skip if this connector isn't the active one in this tab.
+            // 'wagmi.activeConnectorUid' is written to sessionStorage (per-tab) by config.ts
+            // whenever the active connector changes, giving us cross-tab isolation.
+            const _activeUid = typeof window !== 'undefined' ? window.sessionStorage.getItem('wagmi.activeConnectorUid') : null;
+            if (_activeUid && _activeUid !== this.uid) return;
             // Disconnect if there are no accounts
             if (accounts.length === 0)
                 this.onDisconnect();
@@ -410,10 +398,6 @@ export function injected(parameters = {}) {
                     provider.removeListener('connect', connect);
                     connect = undefined;
                 }
-                if (!accountsChanged) {
-                    accountsChanged = this.onAccountsChanged.bind(this);
-                    provider.on('accountsChanged', accountsChanged);
-                }
                 if (!chainChanged) {
                     chainChanged = this.onChainChanged.bind(this);
                     provider.on('chainChanged', chainChanged);
