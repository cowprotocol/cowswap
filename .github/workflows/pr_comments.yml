name: PR comments

on:
  issue_comment:
    types: [created, updated]

jobs:
  pr_commented:
    name: PR comment
    if: ${{ github.event.issue.pull_request }}
    runs-on: ubuntu-latest
    steps:
      - name: Event log
        run: |
          echo $EVENT
        env:
          EVENT: ${{ github.event }}

